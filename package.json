{
  "name": "daphutapea-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site for Daffa Narendra Hutapea. The site itself is plain HTML/CSS/JS; npm is only used to build the Tailwind stylesheet.",
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
